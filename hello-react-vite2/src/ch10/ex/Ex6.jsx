import React from 'react';
// - 각 todo 객체에 `priority` 필드 추가 (`'high'` | `'medium'` | `'low'`)
// - `TodoInsert`에 `<select>`로 우선순위 선택 UI 추가
// - `TodoListItem`에서 우선순위에 따라 왼쪽에 색상 바(border-left) 표시
//     - high: 빨간색 `#ff6b6b`
//     - medium: 노란색 `#fcc419`
//     - low: 초록색 `#51cf66`

const Ex6 = () => {
  return <div></div>;
};

export default Ex6;
